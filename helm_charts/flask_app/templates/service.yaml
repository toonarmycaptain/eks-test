apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask_app.fullname" . }}
  labels:
    {{- include "flask_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask_app.selectorLabels" . | nindent 4 }}
